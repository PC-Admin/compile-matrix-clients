
# Spawn DigitalOcean Droplet
- import_tasks: "{{ role_path }}/tasks/spawn_droplet.yml"
  tags: spawn-droplet

# Check and Save Compile Servers IP
- import_tasks: "{{ role_path }}/tasks/save_server_ips.yml"
  tags: save_ips

# Server Setup
- import_tasks: "{{ role_path }}/tasks/setup_server.yml"
  tags: setup-server

# Setup Developer Environment
- import_tasks: "{{ role_path }}/tasks/setup_developer_environment.yml"
  tags: compile-android

# Compile Element Android
- import_tasks: "{{ role_path }}/tasks/compile_element_android.yml"
  tags: compile-android

# Delete DigitalOcean Droplet
- import_tasks: "{{ role_path }}/tasks/delete_droplet.yml"
  tags: delete-droplet